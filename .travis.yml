# .travis.yml 
language: python
python:
  - "2.7"
env:
  - NODE_VERSION="6.9.1"

before_install:
  - nvm install $NODE_VERSION
  
install: 
  - pip install konlpy
  
before_script:
  - sudo curl -s https://raw.githubusercontent.com/konlpy/konlpy/master/scripts/mecab.sh

script: 
  - mocha unit_test/deudnunda_test.js